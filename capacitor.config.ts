import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
    appId: 'com.purchaseway.animes.app',
    appName: 'Pw Animes',
    webDir: 'dist',
    server: {
        androidScheme: 'https',
    },
}

export default config
